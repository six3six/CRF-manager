{% set knowTopic = topicActive is defined %}
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">CRF-Manager</a>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link"
                   href="{{ path('planning') }}">Mon planning{% if knowTopic and topicActive == "planning" %}<span
                        class="sr-only">(current)</span>{% endif %}</a>
            </li>
            {% if app.user is not null and app.user.admin %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                       data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Administration
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('admin_timeline') }}">Timeline</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path('admin_user_list') }}">Gérer les utilisateurs</a>
                        <a class="dropdown-item" href="{{ path('admin_events_view') }}">Gérer les événements</a>


                    </div>
                </li>
            {% endif %}
        </ul>
    </div>

    {% if app.user is not null %}
        <a href="{{ path('app_login') }}">
            <span class="navbar-text font-weight-bold">{{ app.user.formattedName }}</span>
        </a>

        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_logout') }}">Se d&eacute;connecter</a>
            </li>
        </ul>
    {% endif %}

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>


</nav>